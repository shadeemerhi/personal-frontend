mutation UpdateProject($input: UpdateProjectInput!) {
  updateProject(input: $input) {
    _id
    title
    description
    photoURL
    description
    startDate
    endDate
    inProgress
    repositoryLinks
    stack
    adminPassKey
  }
}
